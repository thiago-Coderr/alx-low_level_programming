#include "main.h"

/**
 * mul - receive two number and multiply tem
 * @a: the first number
 * @b: the second number
 * Return: returns the product of both a anb
 */
int mul(int a, int b)
{
	return (a * b);
}
