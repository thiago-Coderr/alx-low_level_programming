#include "main.h"

/**
 * _isupper - checks every uppercase letters
 * @c: is the parameter of the function
 * Return: will return true of false
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
	
