#include "main.h"
#include <string.h>

/**
 * _strlen - gives thr lenth of a string
 * @s: is the string parameter
 * Return: Always an integer
 */
int _strlen(char *s)
{
	int tam;

	tam = strlen(s);
	return (tam);
}
