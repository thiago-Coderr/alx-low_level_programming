#include "main.h"
#include <string.h>

/**
 * print_rev - prints the string in reversed way
 * @s: pointer parameter
 * Return: void
 */
void print_rev(char *s)
{
	int tam;
	int i;

	tam = strlen(s);
	for (i = 0; i < tam; i++)
	{
		
	}
}
